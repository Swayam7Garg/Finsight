spring:
  application:
    name: finsight-agentic
  ai:
    openai:
      api-key: ${GOOGLE_AI_API_KEY:${GEMINI_API_KEY:dummy-key}}
      base-url: https://generativelanguage.googleapis.com/v1beta/openai/
      chat:
        options:
          model: gemini-2.5-flash

server:
  port: ${AGENT_PORT:5200}

finsight:
  mcp:
    server:
      url: ${MCP_SERVER_URL:http://localhost:5100}
