{
  "buildCommand": "cd packages/shared-types && npm install && npm run build && cd ../../apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
