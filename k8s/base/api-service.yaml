apiVersion: v1
kind: Service
metadata:
  name: finsight-api
  namespace: finsight
  labels:
    app.kubernetes.io/name: finsight-api
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app.kubernetes.io/name: finsight-api
