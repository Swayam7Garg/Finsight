apiVersion: v1
kind: Service
metadata:
  name: wealthwise-api
  namespace: wealthwise
  labels:
    app.kubernetes.io/name: wealthwise-api
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app.kubernetes.io/name: wealthwise-api
