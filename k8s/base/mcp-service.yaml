apiVersion: v1
kind: Service
metadata:
  name: finsight-mcp
  namespace: finsight
  labels:
    app.kubernetes.io/name: finsight-mcp
spec:
  type: ClusterIP
  ports:
    - port: 5100
      targetPort: 5100
      protocol: TCP
  selector:
    app.kubernetes.io/name: finsight-mcp
