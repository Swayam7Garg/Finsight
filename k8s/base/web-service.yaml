apiVersion: v1
kind: Service
metadata:
  name: finsight-web
  namespace: finsight
  labels:
    app.kubernetes.io/name: finsight-web
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: finsight-web
