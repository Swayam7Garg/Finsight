apiVersion: v1
kind: Service
metadata:
  name: finsight-agentic-ai
  namespace: finsight
  labels:
    app.kubernetes.io/name: finsight-agentic-ai
spec:
  type: ClusterIP
  ports:
    - port: 5200
      targetPort: 5200
      protocol: TCP
  selector:
    app.kubernetes.io/name: finsight-agentic-ai
