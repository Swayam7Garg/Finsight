{{- if and .Values.agenticAi.enabled .Values.agenticAi.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "finsight.fullname" . }}-agentic-ai-pdb
  namespace: {{ include "finsight.namespace" . }}
  labels:
    {{- include "finsight.agenticAi.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.agenticAi.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "finsight.agenticAi.selectorLabels" . | nindent 6 }}
{{- end }}
