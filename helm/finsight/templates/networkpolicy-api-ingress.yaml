{{- if .Values.networkPolicies.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "finsight.fullname" . }}-allow-api-ingress
  namespace: {{ include "finsight.namespace" . }}
  labels:
    {{- include "finsight.commonLabels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "finsight.api.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "finsight.web.selectorLabels" . | nindent 14 }}
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
      ports:
        - protocol: TCP
          port: {{ .Values.api.port }}
{{- end }}
